1. git add takes a modified file in your working directory and places the modified version in a staging area. 
2. git commit takes everything from the staging area and makes a permanent snapshot of the current state of your repository that is associated with a unique identifier.
3. git push is used to upload local repository content to a remote repository.
4. git rebase, Rebasing is the process of moving or combining a sequence of commits to a new base commit
5. git merge 
6. git cherrypick enables arbitrary Git commits to be picked by reference and appended to the current working HEAD.
7. git status  
8. git history To view the history of a file in Git, you can use the git log command
9. git diff Diffing is a function that takes two input data sets and outputs the changes between them.
10. git stash


